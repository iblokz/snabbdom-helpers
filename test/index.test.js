'use strict';

const expect = require('chai').expect;

const vdom = require('../index');

describe('snabbdom-helpers', () =>
	describe('patch', () =>
		it('applies a patch to vdom (inherited from snabbdom)')
	)
	&&
	describe('patchStream', () =>
		it('applies a stream of patches to vdom (all streams with .map and .subscribe operators)')
	)
);
